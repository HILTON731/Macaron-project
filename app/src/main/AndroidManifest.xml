<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kangwon.macaronproject">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MacaronProject">
        <activity android:name=".notice_board.PostDetailActivity"></activity>
        <activity android:name=".notice_board.NoticeActivity" />
        <activity android:name=".notice_board.MemberInfoActivity" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity android:name=".SignInActivity"/> -->
        <!-- <activity android:name=".SignUpActivity" /> -->
        <activity android:name=".login.BaseActivity" />
        <activity android:name=".notice_board.MainActivity" android:launchMode="singleTop"/>
    </application>

</manifest>